using System;
using System.Collections.Generic;
using NUnit.Framework;
using ZbW.Testing.Dms.Client.Model;
using ZbW.Testing.Dms.Client.Services;
using ZbW.Testing.Dms.Client.Testing.Mocks;
using ZbW.Testing.Dms.Client.ViewModels;

namespace ZbW.Testing.Dms.Client.Testing.ViewModels
{
    [TestFixture]
    class DocumentDetailViewModelTests
    {
        //[Test]
        //public void RemoveFile_DeleteFile_True()
        //{
        //    // arrange
        //    var messageboxService = new MessageBoxServiceForTesting();
        //    string Username = "User";
        //    Action action = () => messageboxService.Show("Test");
        //    var importService = new ImportServiceForTesting(false);
        //    var exportService = new ExportServiceForTesting(true);
        //    var fileService = new FileServiceForTesting(true, true, true, true);
        //    var searchService = new SearchServiceForTesting(true, true, true);
        //    var itemlist = new List<MetadataItem>();


        //    var DMS = new DocumentManagementService(itemlist, messageboxService, importService, exportService, fileService, searchService);
        //    var documentDetailViewModel = new DocumentDetailViewModel(Username, action, DMS, messageboxService);
        //    documentDetailViewModel.Bezeichnung = "Bezeichnung";
        //    documentDetailViewModel.ValutaDatum = DateTime.Now;
        //    documentDetailViewModel.SelectedTypItem = "Verträge";

        //    // act
        //    var result = documentDetailViewModel.HasRequiredFields();

        //    // assert
        //    Assert.That(result, Is.EqualTo(true));
        //}

        //DocumentDetailViewModel

    }
}
